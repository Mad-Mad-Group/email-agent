# Secrets
.env
.env.*
!.env.example
# Hermes agent 遷移包 —— 內含 ~/.hermes/.env 同 auth.json（API 憑證）
# 同 15k+ 條對話記錄。永遠唔可以入 git，見 scripts/hermes-migrate.sh
hermes-migration-*.tar.gz
*hermes-home*.tar.gz

# 本 repo 統一用 npm（三個 app 都係 package-lock.json，遠端 deploy 亦用 npm ci）。
# yarn.lock 一出現就會同 package-lock.json 漂移，兩個都變唔可信 —— 擋住佢。
yarn.lock

# Node / TS build output
node_modules/
dist/
build/
.cache/
*.tsbuildinfo

# Lead-scraper specific
uploads/
hermes-frontend.zip
ziiqRmns
cms/worker/*.json

# OS / Editor noise
.DS_Store
.vscode/
.idea/
*.swp
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
logs/

# Coverage
coverage/
cms/worker/agent.ts
cms/.DS_Store
AGENTS.md
cms/server/dist/
.DS_Store
